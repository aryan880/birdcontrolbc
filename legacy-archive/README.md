Archived legacy static-site sources that are no longer live in Phase 1.1.

- `root-static/` contains the former root-level HTML/CSS/JS/XML copies.
- `source-static/pages-static/` contains the former `src/pages` copies.
- `source-static/styles-static/` contains the former `src/styles` copies.
- `source-static/scripts-static/` contains the former `src/scripts` copies.

The live legacy fallback surface is now:

- `public/*.html`
- `public/styles.css`
- `public/script.js`
